@using twiview.Locale;
@{
    ViewBag.Title = Locale.Error_Title;
}
<h3>@(ViewBag.Title)</h3>
<ul>
    <li>サインイン時にこの画面が出た場合は、もう一度サインインすると多分直ります。</li>
    <li>URLが壊れている(途中で切れているなど)</li>
    <li>サーバーの調子が悪い</li>
    <li>運が悪い</li>
    <li>(´・ω・`)</li>
</ul>